<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springtest.dao.TestMapper">

    <select id="ge1tPoemName" resultType="String">
        SELECT NAME FROM POET LIMIT 1
    </select>

</mapper>